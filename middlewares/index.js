const {
  contactsValidation,
  contactsValidationForFavirute,
} = require("./contactsValidation");
const isValidId = require("./isValidId");

module.exports = {
  contactsValidation,
  isValidId,
  contactsValidationForFavirute,
};
