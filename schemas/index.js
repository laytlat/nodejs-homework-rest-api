const { contactScheema, updateFavoriteSchema } = require("./contactsSchemas");
const { authSchema, subscriptionSchema } = require("./authSchemas");

module.exports = {
  contactScheema,
  updateFavoriteSchema,
  authSchema,
  subscriptionSchema,
};
