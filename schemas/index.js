const { contactScheema, updateFavoriteSchema } = require("./contactsSchemas");
const {
  authSchema,
  subscriptionSchema,
  verificationSchema,
} = require("./authSchemas");

module.exports = {
  contactScheema,
  updateFavoriteSchema,
  authSchema,
  subscriptionSchema,
  verificationSchema,
};
